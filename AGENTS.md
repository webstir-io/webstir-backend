# AGENTS.md (Repo Stub)

This repository follows the org baseline at `../workspace-tools/AGENTS.md`.

- Scope: Backend module provider (TypeScript → esbuild, entries, watch).
- Start here: `README.md` for provider contract and smoke steps.

